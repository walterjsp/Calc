# Calculadora Web - CI

## Ejecutar
1. `npm install`
2. Abrir `index.html` en el navegador.

## Pruebas
`npm test`

## Validaciones
- `npm run lint:html`
- `npm run check:js`

## Build
`npm run build`

El build crea la carpeta `dist/`.
